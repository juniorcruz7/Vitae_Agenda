#pragma once
#include "TelaInicial.h" // Inclusão do próprio header (redundante devido ao #pragma once)

// Classe View: Define a tela principal da aplicação.
class TelaInicial {
    public:
    // Mostra o menu principal (Agendamentos, Clientes, Sair, etc.)
    void exibirMenu();
};